---
name: value-pyramid
description: Build an Account Value Pyramid — a researched, top-down account strategy deck that cascades from a target company's public vision through its strategic plan, AI/technology programme, and execution proof points, down to the capability gap Stack Internal (or another product) fills. Use this whenever the user asks for a "value pyramid" for an account, asks to map an account's AI initiatives top-down, wants an account strategy deck grounded in a company's public commitments (investor days, CEO initiatives, AI programmes), or says things like "do the same pyramid for another company" after previous pyramid work. Also use for requests to research a prospect's AI strategy and position our product against it.
---

# Account Value Pyramid

Produces two things: a research summary in chat (with citations) and a 7-slide branded .pptx deck. The pyramid method: everything the account has publicly committed to, cascading top-down, ending at the gap our product fills — so the sales argument is built from the customer's own words and numbers.

## Workflow

1. **Research** — read `references/research-guide.md` and follow its search sequence and quality bar. Summarise findings in chat with citations before building anything.
2. **Read the pptx skill** — read `/mnt/skills/public/pptx/SKILL.md` if present (for validation and rendering tooling). The deck itself is generated by this skill's script, not written from scratch.
3. **Fill the config** — copy `assets/example-config.json` (a complete worked example: Santander) and replace the content per the level guidance in the research guide. Structure to preserve:
   - Slide 2 pyramid: exactly 5 levels, top = vision, base = "THE GAP → <PRODUCT>".
   - Slide 5: exactly 6 stats.
   - Slide 6: exactly 4 gap rows plus a positioning line.
   - Slide 7: 4 cards — who to reach, a regional/timing angle, 3 discovery questions, next moves.
   - Speaker notes on every slide (`notes` fields): what to say, not what's on the slide.
4. **Set the brand accent** — `colors.accent` is the account's primary brand colour (hex, no `#`). Provide `accentDark` (readable small text on white) and `accentLight` (readable text on charcoal `2B303A`). If the brand colour is dark, accentLight must be substantially lightened; if light, accentDark must be darkened. Never let accent text fall below comfortable contrast on its background.
5. **Build** — `node scripts/build_deck.js <config.json> <output.pptx>`. Icons are react-icons Feather names (e.g. `FiUsers`); the script fails loudly on unknown names. Slide 8 (objections) is optional — omit `objections_slide` from the config and it will not render.
6. **QA** — validate the file and render every slide to images per the pptx skill's QA section (validate.py, soffice → pdftoppm). Look especially for text overflow in the 6-stat grid and the 4-row gap slide, and low-contrast accent text. Fix in the config, rebuild, re-render changed slides.
7. **Deliver** — copy to the outputs directory and present the file. In chat, give a one-line-per-slide summary plus any caveats (e.g. verify exec names), not a restatement of the deck.

## Judgment calls the script doesn't make

- **Partnerships are assets, not threats.** If the account has a named AI vendor partnership, the gap slide positions the product underneath it, never against it.
- **Facts over adjectives.** If a level can't be filled with dated, quantified, attributable material, say so in chat and thin that level rather than padding it with industry generalities.
- **Shareability.** The deck must contain only public information — no CRM data, private conversations, or internal deal knowledge — so it can be circulated internally without review.
- **Different product?** The default gap arguments are Stack Internal's (see research guide). For another product, keep the argument shape: their-stated-fact → our-mechanism, one row each.
